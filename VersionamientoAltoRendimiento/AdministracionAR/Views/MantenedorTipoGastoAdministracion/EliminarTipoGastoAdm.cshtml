@model AltoRendimiento.Administracion.Models.TipoGastoAdministracionModel

@{
    ViewBag.Title = "Eliminar Tipo Gasto Administración.";
}

<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
    <h4 class="modal-title" id="myModalLabel">¿Está seguro que desea eliminar Tipo Gasto Administración?</h4>
</div>
@using (Html.BeginForm())
{
    <div class="modal-body">
        <div class="form-horizontal">
            <div id="divMensaje">
            </div>
            @Html.ValidationSummary(true)
            @Html.AntiForgeryToken()
            @Html.HiddenFor(model => model.idTipoGastoAdministracion)
            <div class="panel panel-default">
                <div class="panel-heading">
                    Antecedentes Tipo Gasto Personal
                </div>
                <div class="panel-body">
                    <div class="form-group">
                        @Html.LabelFor(model => model.tgaNombre, new { @class = "col-md-6" })
                        <div class="col-md-6">
                            @Model.tgaNombre
                            @Html.HiddenFor(model => model.tgaNombre)
                        </div>
                    </div>
                    <div class="form-group">
                        @Html.LabelFor(model => model.tgaDescripcion, new { @class = "col-md-6" })
                        <div class="col-md-6">
                            @Model.tgaDescripcion
                            @Html.HiddenFor(model => model.tgaDescripcion)
                        </div>
                    </div>
                    <div class="form-group">
                        @Html.LabelFor(model => model.tgaNumeroGastoAdministracion, new { @class = "col-md-6" })
                        <div class="col-md-6">
                            @Model.tgaNumeroGastoAdministracion
                            @Html.HiddenFor(model => model.tgaNumeroGastoAdministracion)
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="modal-footer">
        <button type="button" id="btnCancelar" class="btn btn-sm btn-danger pull-left" data-dismiss="modal">Cancelar</button>
        <button type="submit" id="btnEliminar" class="btn btn-sm btn-success">Eliminar</button>
    </div>
}
<script>
    var codRespuesta = '@(ViewBag.codRespuesta)';
    var msgRespuesta = '@(ViewBag.msgRespuesta)';
    var tipoEstilo = "";
    if (msgRespuesta != "") {
        $("#btnEliminar").attr("disabled", "disabled");
        $("#btnCancelar").html("Cerrar");
        if (codRespuesta == '0') {
            tipoEstilo = "alert-success";
        } else {
            tipoEstilo = "alert-danger";
        }
        $("#divMensaje").append('<div class="alert ' + tipoEstilo + ' alert-dismissable"><button type="button" class="close" data-dismiss="alert">&times;</button>' + msgRespuesta + '</div>');
    }
</script>

