@model AltoRendimiento.Administracion.Models.MantenedorDisciplinaModel

@{
    ViewBag.Title = "Eliminar Disciplina.";
}

<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
    <h4 class="modal-title" id="myModalLabel">¿Está seguro que desea eliminar la Disciplina?</h4>
</div>
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div class="modal-body">
        <div class="form-horizontal">
            <div id="mensaje">
            </div>
            @Html.ValidationSummary(true)
            @Html.HiddenFor(model => model.IdDisciplina)
            <div class="form-group">
                @Html.LabelFor(model => model.Nombre, new { @class = "col-md-2" })
                <div class="col-md-10">
                    @Model.Nombre
                    @Html.HiddenFor(model => model.Nombre)
                </div>
            </div>
            <div class="form-group">
                @Html.LabelFor(model => model.Descripcion, new { @class = "col-md-2" })
                <div class="col-md-10">
                    @Model.Descripcion
                    @Html.HiddenFor(model => model.Descripcion)
                </div>
            </div>
            <div class="form-group">
                @Html.LabelFor(model => model.orgNombre, new { @class = "col-md-2" })
                <div class="col-md-10">
                    @Model.orgNombre
                </div>
            </div>
        </div>
    </div>
    <div class="modal-footer">
        <button type="button" id="btnCancelar" class="btn btn-sm btn-danger pull-left" data-dismiss="modal">Cancelar</button>
        <button type="submit" id="btnEliminar" class="btn btn-sm btn-success ">Eliminar</button>
    </div>
}
<script>
    var codError = '@(ViewBag.codError)';
    var mensajeRespuesta = '@(ViewBag.respuesta)'
    var tipoEstilo = "";
    if (mensajeRespuesta != "") {
        $("#btnEliminar").attr("disabled", "disabled");
        if (codError == '0') {
            tipoEstilo = "alert-success";
            $("#btnCancelar").html("Cerrar");
        } else {
            tipoEstilo = "alert-danger";
        }
        var mensajeUsuario = '<div class="alert ' + tipoEstilo + ' alert-dismissable"><button type="button" class="close" data-dismiss="alert">&times;</button>' + mensajeRespuesta + '</div>';
        $("#mensaje").append(mensajeUsuario);

    }
</script>